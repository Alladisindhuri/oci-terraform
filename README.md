# oci-terraform
"Infrastructure-as-Code (IaC) repository for Oracle Cloud Infrastructure. Contains Terraform configurations and reusable modules for provisioning core network (VCN), security lists, compute instances, and storage resources.

This is repo to create Terraform script.